TASK 1: /ui-ux-pro-max:ui-styling /ui-ux-pro-max:ui-ux-pro-max make the frontend professionala nd perfect logical flow and fix overlap and outofbox cotnent and proper buttons placing and perfect allignment

TASK 2: /cavecrew /ui-ux-pro-max:ui-ux-pro-max /ui-ux-pro-max:ui-styling /implement Add a second, coarser clustering level so the map supports alphaXiv-style zoom-to-reveal: zoomed out shows domain-level blobs (existing classification.py domains), zooming into one reveals its HDBSCAN sub-clusters (existing clustering.py output) as the current view already renders them. Don't re-cluster — this is a display hierarchy over data you already have: domain (parent) → existing cluster (child). Add a parent_domain field to the cluster response in map.py (already derivable from clusters.domain, no schema change needed). On the frontend, MapView.tsx should render only domain-level aggregate blobs by default (sum member repos per domain, single label, single color), and on click/zoom-in, transition to showing that domain's existing sub-clusters at full detail — reuse the current force-simulation code for the zoomed-in view, don't rewrite it. Add a breadcrumb (All / <Domain>) above the map for zoom-out. Do not touch layout.py's MDS/PCA math or the clustering algorithm — this is purely a two-level rendering mode over existing cluster/domain data. 
