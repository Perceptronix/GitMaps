There are two reference projects extracted into the Reference/ folder (already gitignored — confirm it still is, since it holds new content now): TicketTagger and ghelephant. Analyze both deeply and report back before changing any GitMaps code. Do not copy code verbatim from either — check each project's LICENSE file first and report what it is; anything copied must be license-compatible and attributed, and given ADR-0002's transparency requirement, GitMaps' classification stays rule-based, not a trained model, so TicketTagger's ML approach is a reference for the problem shape only, not a component to port in.

For Reference/ghelephant: C:\Users\nande\OneDrive\Desktop\GitMaps\Reference\ghelephant-main

Read its full ingestion pipeline: how it downloads GH Archive hourly JSON dumps, what event types it parses (PushEvent, WatchEvent/stars, ForkEvent, IssuesEvent, etc.), and how it maps them into its Postgres schema.
Compare its schema against supabase/migrations/ — specifically snapshots and repos tables. Identify exactly which GH Archive event types would let us backfill historical snapshots rows (star growth over time) for repos we've only just started tracking, instead of waiting weeks for our own daily snapshot cadence to accumulate.
Propose (don't implement yet) a design for a one-time or periodic GH Archive backfill job that writes into our existing snapshots table via RepoStore, following the same Runner pattern as SnapshotRunner/MomentumRunner — pure engine + store seam + ingestion_state progress tracking, consistent with every other pipeline stage. Flag any schema gaps (e.g., does snapshots have room for arbitrary historical timestamps, or does it assume "now"?).
Note ghelephant's storage requirements (its README mentions ~100GB temp space) and propose how to scope a backfill to be reasonable for a self-hosted setup — e.g., last N months only, or only for repos already in our surfaced set, not the full GH Archive firehose.

For Reference/TicketTagger: C:\Users\nande\OneDrive\Desktop\GitMaps\Reference\ticket-tagger-main

Read how it structures its issue-classification training data and feature extraction — even though we won't adopt ML classification, the feature engineering (what signals from an issue/repo's text are predictive) might inform better keyword selection for classification.py's DEFAULT_TAXONOMY.
Report whether it has any rule-based fallback or baseline logic (many ML classifiers ship one) that's directly comparable to our word-bounded regex approach — if so, compare precision/recall tradeoffs conceptually.
Do NOT propose swapping our classifier for a trained model. Report findings only; end with a short list of concrete, small taxonomy/keyword improvements to classification.py that the comparison suggests, each justified by evidence from TicketTagger's approach.

Deliver a written findings report (no code changes) covering both, then wait for confirmation before implementing anything.