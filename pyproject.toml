[project]
name = "gitmaps"
version = "0.1.0"
description = "AI-powered GitHub intelligence platform — collector worker."
requires-python = ">=3.11"
dependencies = [
    "requests>=2.31",
    "psycopg2-binary>=2.9",
    "sentence-transformers>=3.0",
    "numpy>=1.26",
    "scikit-learn>=1.3",  # HDBSCAN for the clustering pipeline (Phase 7) — no numba dependency
]

[project.optional-dependencies]
dev = [
    "pytest>=8",
    "mypy>=1.10",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]

[tool.mypy]
python_version = "3.11"
packages = ["gitmaps"]
warn_unused_configs = true
